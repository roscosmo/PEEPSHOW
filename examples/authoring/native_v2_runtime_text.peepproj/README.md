# Native V2 Runtime Text

API 49 source fixture for Studio preview and normal public export.

- A enters `Moved and hidden`: `Aa` moves right and `Visible` is hidden.
- B returns to `Initial`.
- The numbered four-frame animation continues across both state changes.
- All labels are genuine runtime system-font text objects. Only the numbered animation uses assets.
