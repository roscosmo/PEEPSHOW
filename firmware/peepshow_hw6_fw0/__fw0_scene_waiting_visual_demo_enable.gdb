set pagination off
set var g_display_renderer_waiting_test_variant = 0
set var g_display_renderer_scene_waiting_demo_enable = 1
printf "--- HW6 production 2-phase + 3-phase scene proof enabled ---\n"
printf "Resume and press L or R once so the settled shell scene republishes.\n"
printf "Expected: cursor continues blinking while the edge marker cycles through three visible positions at 250 ms steps awake and in STOP2.\n"
